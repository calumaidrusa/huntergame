# 族語射手 — 五關五玩法改版設計規格 v1

> 2026-07-04 定案。關卡從「難度階梯」改為「玩法階梯」：單詞全難度混合出題，
> 五關各是一種學習玩法，難度逐關升高。**保留順序解鎖**（過關才開下一關），
> 累計排行榜與帳號系統不變。

---

## 0. 核心架構轉變

| 舊設計 | 新設計 |
|---|---|
| L1-L4 = 詞彙難度（初/中/高/地獄） | L1-L5 = **玩法**（刷透明/填空/聽打/看圖選詞/盲打） |
| 每關只出該難度的詞 | 每關從**全難度混合池**抽詞 |
| 4 關 | **5 關** |
| 順序解鎖 ✅ | 順序解鎖 ✅（不變） |
| 累計排行榜 ✅ | 累計排行榜 ✅（不變） |

**單一玩家旅程**：登入 → L1 刷透明過關 → 解鎖 L2 填空 → … → L5 盲打畢業。
每關詞彙隨機、混難度，重點是換一種「認知負荷」練同一批詞。

---

## 1. 五種玩法定義

出題共用資料：每題一個 prey 物件 `{ word(族語), zh(中文), category, emoji, image, audio, words[] }`。
判定一律走現有 `normalizeInput()`（去頭尾空白、壓多空格、小寫）。多字詞沿用整句比對。

### L1 —「音選詞」Audio Multiple-Choice（入門）〔2026-07-04 使用者改版：原「刷透明」改為此〕
- **呈現**：**不顯示族語詞**，自動播音檔 + 大播放鈕（可重播），底下給 **4–6 個相似族語單字選項按鈕**，玩家聽發音後**點選**正確的那個。可搭配中文/圖當輔助提示。
- **輸入**：**點選**（非打字）。與 L4 共用同一套「選項按鈕群 + 點選判定」UI，差別只在提示線索是「音檔」而非「圖片」。
- **判定**：點對 = caught + 射箭；點錯 = miss + 扣血，正確選項標綠、錯的標紅短暫提示。
- **誘答演算法**：同 L4（開頭字母相同/長度相近/編輯距離小/同 category）。
- **資料前提**：**只出有音檔的詞**（klokah 音檔 100%，池子夠）。
- **入門定位**：這是最簡單的一關（聽 + 認，不用產出拼寫），適合初學者與手機觸控。
- ＊原「刷透明」玩法先移除（若之後想保留可當額外關卡再議）。

### L2 —「填空」Fill the Blank（初階）
- **呈現**：族語詞挖掉部分字母以底線代替，例如 `m_t_qi`（挖 30–40% 字母，位置隨機但保留首字母）。中文/圖持續顯示。
- **輸入**：玩家打**完整**單詞（不是只打缺字母，較簡單且判定一致）。＊備選：只補缺字母——但會讓多字詞與判定複雜，v1 先打完整詞。
- **判定**：完整比對族語詞。即時上色時，已顯示的字母視為已知。
- **資料前提**：任何詞（單詞長度 ≥ 3 才挖，太短的整個顯示或改別題）。
- **沿用**：輸入流程沿用；新增「挖空遮罩」渲染。

### L3 —「聽打」Listen & Type（中階）
- **呈現**：**不顯示族語詞**，只顯示中文/圖 + 大播放鈕，自動播音檔，玩家聽發音打出族語。答對後顯示正確詞 0.8 秒。
- **輸入**：底部輸入列打字。
- **判定**：完整比對族語詞。
- **計時**：×1.5（聽需要時間，同現有聽聲辨字）。
- **資料前提**：**只出有音檔的詞**（klokah 音檔覆蓋 100%，基本不缺）。
- **沿用**：**這關就是現有「聽聲辨字」模式**（`G.listenMode` 那套 `listenReveal`/`listenPlayBtn`/計時×1.5/BGM 暫停/跳過無音檔）。直接把它接成 L3，幾乎免重寫。

### L4 —「看圖選詞」Picture Multiple-Choice（進階）
- **呈現**：大圖 + **4–6 個相似族語單字選項按鈕**，**不播音檔**。玩家點選正確的那個。
- **輸入**：**點選**（不是打字！這關唯一非打字關）。
- **判定**：點對 = caught + 射箭；點錯 = miss + 扣血，錯的選項標紅、正確標綠短暫提示。
- **誘答演算法**：正解 + 3–5 個「相似詞」。相似度依序取：① 開頭字母相同 ② 長度相近(±1) ③ 編輯距離小（Levenshtein ≤2）④ 同 category。前端從混合池即時挑，不夠再放寬條件。避免選項重複、避免出現同義。
- **資料前提**：**只出有圖片的詞**（image_path 非空，約 600/1092 有圖 → 出題池夠）。
- **素材/UI 需求**：選項按鈕底圖（可沿用 `v2-button-green-trim` 或新做木牌選項鈕）；正解/誤答的綠框/紅框回饋。**需小畫家評估要不要新素材**。
- **沿用**：射箭命中特效沿用（點對就 fireArrow）；但輸入層是全新的「選項按鈕群」，底部輸入列這關隱藏。

### L5 —「看中文盲打」Blind Production（優級 / 畢業關）
- **呈現**：**只給中文意思（可加圖）**，完全不顯示族語詞、不播音、不給選項。玩家憑記憶打出族語。
- **輸入**：底部輸入列打字。
- **判定**：完整比對族語詞。
- **計時/計分**：**最短計時、扣血最重、題數最多**，最高分倍率。
- **資料前提**：任何詞。
- **沿用**：輸入/射箭流程沿用；本質是 L1 拿掉「先看一眼」的最難版。
- ＊備選方案（待用戶決定要不要換）：**綜合隨機關**——每題隨機抽 L1–L4 之一，像大魔王混戰。

---

## 1.5 學習強化機制（2026-07-04 使用者新增，跨所有關卡）

### (A) 關卡內單詞重複出現（間隔複習）
- 目的：**盡可能達到學習效果**——不是一關塞一堆詞各出一次，而是**聚焦一小組詞、讓它們重複出現**。
- 規則建議（小工程可微調）：每關先抽 **K 個不重複的詞**（例如 K=8~10），關卡題數 `totalRounds` 設成大於 K（例如 15~20），使同一個詞在一關內自然出現多次。
- **答錯優先重現**：玩家答錯/逾時的詞，把它插回「近端佇列」讓它較快再出一次（簡易間隔複習）；答對的詞出現間隔拉長。
- 不影響計分/解鎖/排行榜邏輯（只改出題佇列的組成方式）。

### (B) 關卡結束單詞回顧（Game Over + Stage Clear 都要）
- 需求原話：「每一次結束關卡，可以看到剛剛出題的單詞，以及我錯誤的地方」。
- **逐題結果追蹤**：遊戲進行中記錄每一題 `{ word, zh, mode, result: correct|wrong|timeout }` 到 `G` 狀態（新陣列，例如 `G.roundLog`），initLevel 時清空。
- **結算畫面新增回顧區**：在 Game Over / Stage Clear 面板加一個可捲動清單，列出這關出過的**單詞（去重）**＋中文，並**標記答錯的**（紅字/✗），答對的正常（綠/✓）。可顯示每個詞「出現 N 次、對 M 次」。
- 版面：塞在現有統計卡下方或做成可展開區塊；文字不寫死，資料驅動。小排版之後負責美化，小工程先把資料與基本清單接出來。

---

## 2. 後端改動（小工程，範圍很小）

`backend/server.js`：
1. `const MAX_LEVEL = 4;` → **`5`**（解鎖上限；getUnlockedLevel/防繞過/排行榜 rank 全自動跟著支援第 5 關，無需其他改動）。
2. `/api/vocabulary` 混合出題：前端改成**不帶 level 參數**即回全池（現有 `WHERE 1=1` 已支援）。另**新增兩個過濾參數**方便前端出題：
   - `?hasImage=1` → `AND image_path IS NOT NULL AND image_path <> ''`（給 L4）
   - `?hasAudio=1` → `AND audio_path IS NOT NULL AND audio_path <> ''`（給 L3）
3. `scores` 表、`cleared`、`player_id`、排行榜 SQL **全部不動**。真實玩家資料零風險。
4. `vocab_seed.js` 種子資料不動（詞彙內容不變，只是出題方式變）。

＊部署鐵律照舊：先備份 db、絕不動 scores 表、純後端改動才需 `pm2 restart hunter-api`。

---

## 3. 前端改動（小工程 + 小排版）

`hunter-truku-v2.html`：
1. **LEVEL_CONFIG 改成 5 關**，每關加 `mode` 欄位：
   ```
   1: { mode:'fade',   label:'LEVEL 1: 刷透明', ... 寬鬆 }
   2: { mode:'blank',  label:'LEVEL 2: 填空',   ... }
   3: { mode:'listen', label:'LEVEL 3: 聽打',   ... 計時×1.5, hasAudio }
   4: { mode:'choice', label:'LEVEL 4: 看圖選詞', ... hasImage, 非打字 }
   5: { mode:'blind',  label:'LEVEL 5: 盲打',   ... 最難 }
   ```
2. **出題資料層**：`loadVocabulary` 依 mode 決定要不要帶 `hasImage`/`hasAudio`，並從全池混合抽題（不再按 level 難度）。
3. **nextRound() 分支到各 mode 的渲染/輸入邏輯**：抽出 `renderRoundByMode(mode, prey)`，把現有打字流程當 fade/blank/listen/blind 的共用底，choice 走獨立分支（選項群 + 點選判定，隱藏底部輸入列）。
4. **判定統一**：打字四關共用 `submitWord()`；choice 關新增 `selectChoice(word)`。兩者都在對的時候呼叫 `fireArrow()` + `caughtPrey()`。
5. **關卡選擇畫面 / GameOver / StageClear** 的關卡名稱、難度徽章跟著改成玩法名（素材可能要補：現有 `v2-difficulty-{beginner/intermediate/advanced/hell}` 只有 4 個徽章，5 關要第 5 個；**小畫家評估**）。

---

## 4. 需要小畫家的素材（待評估）
- 第 5 關難度徽章（現有只有 4 個）。
- L4 選項按鈕樣式 + 正解/誤答綠框紅框回饋（或沿用現成綠鈕）。
- 五關玩法可能各要一個小圖示（刷透明/填空/耳朵/圖卡/盲眼），關卡選單用——非必要，先文字。

---

## 5. 建議上線順序（分階段，每階段可獨立驗證+部署）
1. **Phase A — 後端 + 資料層**：MAX_LEVEL→5、hasImage/hasAudio 參數、loadVocabulary 混合出題。先讓現有打字玩法在「混合詞池」下正常跑。
2. **Phase B — L3 聽打接線**：把現有聽聲辨字模式接成 L3（最省力，先拿一關驗證 mode 架構）。
3. **Phase C — L1 刷透明 + L2 填空**：純打字加渲染變化，風險低。
4. **Phase D — L5 盲打**：最省，L1 拿掉提示。
5. **Phase E — L4 看圖選詞**：最大工程（非打字、誘答演算法、新 UI），最後做。
6. 每階段：本機 preview 驗證 → 用戶確認 → 部署（scores 不動）。

---

## 6. 待用戶最終拍板
- **L5 玩法**：預設「看中文盲打」，或改「綜合隨機關」？
- **L2 填空輸入**：打完整詞（v1 建議）還是只補缺字母？
- **L4 選項數**：4 / 5 / 6 個？
- L4 無圖的詞怎麼辦：已決定「L4 只從有圖的詞出題」。
