# Hunter Typer: Forest Chase — 手機版「點字拼字」模式實作規格

## 0. 給實作者(agent)的前提

本文件是**設計意圖 + 硬性限制**,不是逐行 diff。先檢視現有專案結構,再依原則實作。任何與「絕對限制」衝突的做法一律不採用。

本專案的路線已定案:**手機版「再做一份」獨立實作,不與桌機共用遊戲邏輯**。共用的只有「靜態素材」與「單字資料」,且以引用同一份來源為準,不實體複製。

---

## 1. 目標

替 Hunter Typer 新增一個**手機專用版本**:直式畫面、點選字母磚拼字、目標由上而下逼近、往上射擊。桌機維持原本的橫式鍵盤打字,完全不變。

---

## 2. 絕對限制(最重要,先讀這段)

1. **桌機版現有檔案一個字都不能動。** 不改桌機的 HTML / JS / CSS,不改桌機的鍵盤監聽、判定、版面、難度、構圖。
2. 手機版用**獨立入口與獨立檔案**實作(例如 `mobile.html` + `game-mobile.js` + `mobile.css`),自帶手機專用的遊戲迴圈與版面。與桌機互不進入。
3. **手機版擁有自己的遊戲邏輯**(移動軸、敵人路徑、判定、難度、版面各自獨立)。不呼叫桌機的核心函式,避免共用到桌機專屬的假設(讀鍵盤狀態、打字速度算分、桌機才有的 DOM)。
4. **可共用的只有兩類「靜態、不執行」的東西**,且以引用同一份來源為準、不實體複製:
   - **素材**:sprite、圖片、音效、字型。
   - **單字資料**:單字庫、關卡設定等純資料。
5. 若單字資料目前寫死在桌機主程式內,允許的唯一「碰桌機」動作是:把**純資料**抽成獨立檔(例如 `words-data.js`),桌機改為 import。僅限搬移資料、不動邏輯,搬完必須確認桌機行為完全一致。若無法安全抽出,手機端維護一份副本並在檔案開頭註明「需與桌機資料同步」。

> 共用原則一句話:**靜態的(素材、資料)共用引用;會執行的(邏輯、難度、版面、構圖)各自隔離。**

---

## 3. 裝置分流

- 進入頁面時偵測裝置:
  - 觸控裝置 / 視窗寬度小於門檻(建議 `<= 820px` 或 `pointer: coarse`)→ 導向手機版。
  - 其餘 → 走現有桌機流程,不做任何改動。
- 偵測用 `window.matchMedia('(pointer: coarse)')` 搭配寬度判斷,不要只靠 UA 字串。
- 提供手動切換入口(小按鈕或網址參數 `?mode=mobile` / `?mode=desktop`),方便測試與例外情況。

---

## 4. 直式版面與縱向移動軸(手機版的核心差異)

桌機是橫式(寬 > 高),敵人左右進場、橫向逼近。手機直式(高 > 寬),橫向被壓扁、縱向空間多,**移動軸整個從水平轉為垂直**,不是把橫式縮小塞進去。

### 4.1 移動軸與構圖
- **獵人固定在底部、朝上**。
- **敵人 / 目標從畫面頂部生成,往下逼近**;逼近距離用畫面高度算,不是寬度。
- **拼完字 → 箭往上飛**擊中目標。
- **危險線(扣血 / game over 線)**設在底部獵人前方一段距離,敵人越線才觸發;這條線離字母磚要留手感距離,不要一越線就懲罰。
- canvas 內的背景、地面、獵人站位為**直式重新擺位**(用同一批 sprite,重新 layout),不要把橫式構圖硬拉成直式導致變形或大片留白。

### 4.2 版面配置(由上到下)
- **頂部固定列**:HP / SCORE / COMBO。
- **中段遊戲區(canvas)**:依螢幕寬高自適應縮放,維持長寬比,不變形;佔畫面最大區塊,承載「敵人由上而下」的縱向空間。
- **底部操作區**:字母磚 + clear 鈕,落在拇指容易點到的範圍。
- 確認 `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`;處理安全區 `env(safe-area-inset-*)`。
- 鎖定直式,或在橫式時給合理提示。

### 4.3 觸控目標
- 所有可點元素(字母磚、clear、replay、關閉等)**至少 44×44px**,磚與磚間留足間距避免誤觸。

---

## 5. 素材沿用原則

- **sprite / 圖片 / 音效 / 字型:沿用桌機同一批,引用同一份來源,不實體複製。**(哪天換一張圖不用改兩邊。)
- **字母磚是唯一新增的視覺元件**:桌機打字沒有此物。它是帶字母的按鈕,需要「正常 / 選中 / 鎖定 / 禁用」四種狀態樣式,配色與遊戲風格一致即可,非美術等級工作。
- **可能需要補的素材只有兩項**(先確認現有素材有沒有,沒有再補):
  1. **朝上的獵人 sprite**:現有 sprite 若只有朝左右,直式朝上需要鏡像或新增朝上姿勢。
  2. **直式背景**:桌機若是橫幅式背景,直式可能左右不夠、上下太多,需為直式重摆或補一張可延伸的背景。
- 除上述兩項,**美術零重做**。

---

## 6. 手機版核心機制:點字拼字

### 6.1 流程
1. 目標單字出現時,取得該單字字串。
2. 把字母**打散(shuffle)**成一排字母磚,顯示在底部操作區。
3. 玩家依**正確順序**點字母磚拼出單字。
4. 點對 → 該字母鎖定、進度前進一格,已用磚變灰 / 禁用。
5. 拼完整個字 → 觸發手機版自己的「單字完成」流程:往上射箭、擊中目標、加分、combo +1。

### 6.2 第一版難度(先簡單,可迭代)
- 字母磚**只放正確字母打散**,不加干擾字母。難度靠敵人由上而下的時間壓力。
- 之後版本再加入干擾字母(distractors)。

### 6.3 必須處理的細節
- **重複字母**:單字含兩個以上相同字母時,判定以「位置 / 進度」為準,不能只比對字元,避免點錯磚卻算對。
- **太魯閣語特殊字元**:字母磚要正確顯示特殊符號,字型 / 編碼確認無誤。
- **點錯回饋**:第一版「點錯不扣血,只給視覺 + 震動回饋(`navigator.vibrate`)」,避免手機誤觸就懲罰。是否扣血列為可調參數。
- **退格 / 重來**:提供 clear 鈕清除當前拼字,或點錯自動不前進。

### 6.4 難度與節奏(手機獨立)
桌機用打字速度當難度,手機用點擊速度 + 敵人下降速度。**敵人生成 / 下降速度、危險線位置、每字母磚數量等參數全部手機獨立維護**,調手機不影響桌機。分數 / combo 的規則若要與桌機一致,可**參照桌機的數值與公式重寫一份**,但不共用桌機函式。

---

## 7. 驗收清單

- [ ] 桌機版行為與改動前**完全一致**(鍵盤打字、判定、版面、分數、構圖逐項對照)。
- [ ] 手機開啟自動進入手機版,直式版面正常。
- [ ] 縱向移動軸正確:獵人底部朝上、敵人由上而下、往上射擊、危險線在底部。
- [ ] 字母磚打散顯示,依序點對可拼完單字;拼完觸發射箭 / 得分 / combo。
- [ ] 重複字母的單字判定正確。
- [ ] 太魯閣語特殊字元正確顯示。
- [ ] 所有可點元素 ≥ 44px,無明顯誤觸。
- [ ] canvas 在不同手機尺寸下自適應、不變形,直式構圖不留大片空白。
- [ ] 素材為共用引用(非複製);單字資料共用或已註明需同步。
- [ ] 若補了朝上獵人 sprite / 直式背景,風格與原版一致。

---

## 8. 不要做的事(guardrails)

- 不要為了「順便優化」去重構桌機程式。
- 不要呼叫桌機的遊戲核心函式(手機自帶邏輯,避免共用桌機假設)。
- 不要把橫式構圖直接縮放塞進直式,導致擠成一團或變形。
- 不要把桌機的打字輸入改成也走點字。
- 不要為了做手機版而改動桌機的資料結構語意(只允許安全搬移純資料)。
- 素材不要實體複製,用引用共用。

---

## 9. 交付

- 新增手機版入口與相關檔案(獨立於桌機)。
- 若搬移過共用資料,附上「桌機行為未變」的確認說明。
- 列出手機版新增 / 可調的參數(敵人下降速度、危險線位置、每字母磚數量、是否扣血等)。
- 列出是否補了朝上獵人 sprite / 直式背景。
